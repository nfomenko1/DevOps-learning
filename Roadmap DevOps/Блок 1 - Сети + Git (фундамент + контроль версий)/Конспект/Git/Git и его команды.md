🟢 **БАЗОВЫЕ**:
`git init` - Создать новый репозиторий
`git status` - Показать состояние (что изменено)
`git add .` - Добавить все файлы в индекс
`git add файл` - Добавить конкретный файл
`git commit -m "текст"` - Сохранить изменения
`git push` - Отправить на GitHub
`git remote add origin URL` - Привязать к GitHub

**🟡 ВЕТКИ (branches)**  
`git branch` - Показать все ветки  
`git branch имя` - Создать новую ветку  
`git checkout имя` - Переключиться на ветку  
`git checkout -b имя` - Создать + переключиться  
`git switch имя` - Переключиться (новый способ)  
`git branch -d имя` - Удалить ветку

**🔴 РАБОТА С УДАЛЁННЫМИ**  
`git remote -v` - Показать удалённые репозитории  
`git pull` - Скачать + слить изменения  
`git push origin main` - Пуш конкретной ветки  
`git push -u origin main` - Пуш + запомнить связь

**🟣 ИСТОРИЯ И ПРОСМОТР**  
`git log` - История коммитов  
`git log --oneline` - Короткая история  
`git diff` - Показать изменения

**⚠️ ОТОМЕНЫ И ИСПРАВЛЕНИЯ**  
`git checkout -- файл` - Отменить изменения файла  
`git restore файл` - То же (новый способ)  
`git reset --hard` - Отменить все изменения  
`git commit --amend` - Изменить последний коммит

**🔵 КЛОНИРОВАНИЕ И КОПИИ**  
`git clone URL` - Скачать чужой репозиторий  
`git stash` - Спрятать изменения  
`git stash pop` - Вернуть спрятанное
