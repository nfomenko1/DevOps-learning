# Block 1 — Networks & Git (конспект)

## DNS
- Что такое DNS.
- Что показывает nslookup google.com.

## Ping
- Что значит отправлено/получено/потеряно.
- Что такое задержка (мс).

## HTTP
- Что делает curl https://httpbin.org/ip.

## Git
- Кратко: git init, git status, git add, git commit.
